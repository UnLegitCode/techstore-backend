spring:
  application:
    name: techstore-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/techstore_db
    username: unlegit
    password: aFdhH843CpfF
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: none

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /api-docs/ui
    operations-sorter: method
    tags-sorter: alpha
    display-request-duration: true
  show-actuator: false

app:
  jwt:
    secret: abcdefghijklmnoprstuvwxyz1234567890
    expiration-ms: 86400000 # 24 часа